server:
  port: 8090
spring:
  application:
    name: backend
  data:
    redis:
      url: ${LOG_REDIS_URL:redis://redis:6379}   # 미지정 시 Spring 기본 localhost = 컨테이너 자신 (함정)
management:
  endpoints:
    web:
      exposure:
        include: health
